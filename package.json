{
  "name": "chrome-ranger",
  "version": "0.1.0",
  "description": "CLI orchestrator for running scripts against a matrix of Chrome versions and git refs",
  "type": "module",
  "bin": {
    "chrome-ranger": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": ["chrome", "testing", "benchmark", "matrix"],
  "author": "Ben Clinkinbeard",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "dependencies": {
    "@puppeteer/browsers": "^2.0.0",
    "commander": "^13.0.0",
    "js-yaml": "^4.1.0"
  }
}
